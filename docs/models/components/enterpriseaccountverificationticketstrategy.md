# EnterpriseAccountVerificationTicketStrategy

## Example Usage

```ruby
require "clerk-sdk-ruby"

value = EnterpriseAccountVerificationTicketStrategy::TICKET

# Open enum: use .deserialize() to create instances from custom string values
custom = EnterpriseAccountVerificationTicketStrategy.deserialize("custom_value")
```


## Values

| Name     | Value    |
| -------- | -------- |
| `TICKET` | ticket   |